---
import BtnColor from "@components/BtnColor.astro";

interface Props {
    key: string;
}

const { key } = Astro.props;

const isPassword = key == "Contraseña";
---

<div id={key}>
    <h5>{key}</h5>
    <hr />
    {
        isPassword ? (
            <span>
                <input type="password" value={"Contraseña"} readonly />
                <BtnColor text="Cambiar" type="change_password" />
            </span>
        ) : (
            <p />
        )
    }
</div>

<style>
    div {
        position: relative;

        border-radius: 8px;
        padding: 10px;
        margin: 10px;
        background-color: #454545;

        html.light & {
            background-color: #b8b8b8;
        }

        h5 {
            color: #989898;

            html.light & {
                color: #454545;
            }
        }

        hr {
            height: 1px;
            margin-top: 1px;

            background-color: #989898;
            box-shadow: none;
        }

        p {
            color: var(--text-color-dark);

            html.light & {
                color: var(--text-color-light);
            }
        }

        & > span {
            position: relative;
            display: flex;
            justify-content: space-between;
            align-items: center;

            input {
                height: 19px;
                width: calc(100% - 100px);

                border: none;
                color: inherit;
                cursor: default;
                font-size: 1.5rem;
                background-color: transparent;

                &:focus {
                    outline: none;
                }
            }
        }
    }
</style>
<script>
    document.querySelector(".change_password").addEventListener("click", () => {
        console.log("hola");
    });
</script>
