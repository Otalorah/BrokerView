<form class="form" id="form__login">
	<div class="form__group">
		<label class="form__label" for="user">Usuario:</label>
		<input class="form__input" type="text" id="user" name="user" required />
	</div>

	<div class="form__group">
		<label class="form__label" for="Password">Contraseña:</label>
		<div class="form__group-input">
			<input
				type="password"
				class="form__input"
				name="password"
				id="password"
			/>
			<i class="password_icon fa-solid fa-eye-slash"></i>
		</div>
		<p class="form__text forget-password">
			<a class="text__link" href="src/pages/signin.astro"
				>¿Has olvidado tu contraseña?</a
			>
		</p>
	</div>

	<div class="form__group">
		<div class="background__btn">
			<span class="backfill__btn"></span>
			<button class="btn" id="submit" type="submit">INGRESAR</button>
		</div>
	</div>
</form>
<style>
	.form {
		position: relative;
		display: grid;
		grid-template-columns: 1fr;
		padding: 20px;
		border-radius: 8px;
		width: 295px;
		z-index: 50;
		color: #fff;
		background-color: #222;

		&#form__login {
			height: 275px;
		}
		&#form__signin {
			height: 625px;
		}

		.form__label {
			display: block;
			margin-bottom: 8px;
		}

		.form__group-input {
			position: relative;
			margin: 0;

			.status_icon {
				position: absolute;
				right: 0.55rem;
				top: 0.6rem;
				opacity: 0;
				color: #0ff;
			}
			.password_icon {
				position: absolute;
				cursor: pointer;
				right: 0.55rem;
				top: 0.6rem;
				color: #222;
			}
		}

		.form__input {
			border: none;
			width: 100%;
			padding: 8px;
			margin-bottom: 10px;
			border-radius: 4px;

			&:focus {
				outline: none;
			}
		}

		.form__text {
			text-align: center;
			margin: 0.05rem;

			&.forget-password {
				text-align: end;
				margin: 0;
			}

			.text__link {
				font-size: 0.8rem;
			}
		}
	}

	.background__btn {
		position: relative;
		display: flex;
		justify-content: center;
		align-items: center;
		border-radius: 4px;
		margin: auto;
		margin-top: 5px;
		width: 90%;
		height: 40px;
		background: #fff;

		&:hover {
			background: linear-gradient(to right, #0f0, #ff0, #0ff, #f0f);
		}

		&:hover .backfill__btn {
			display: block;
		}

		&:hover .btn {
			background-clip: text;
			color: transparent;
			background-image: linear-gradient(
				to right,
				#0f0,
				#ff0,
				#0ff,
				#f0f,
				#0ff
			);
		}

		.backfill__btn {
			display: none;
			height: 90%;
			width: 98%;
			background-color: #222;
		}

		.btn {
			border: none;
			cursor: pointer;
			position: absolute;
			font-weight: bold;
			display: block;
			z-index: 20;
			width: 100%;
			border-radius: 4px;
			height: 90%;
			width: 98%;
			color: #fff;
			background: #222;
		}
	}
	.text__link {
		text-decoration: none;
		margin: 1.5rem auto;
		font-size: 1rem;
		color: #fff;
	}
</style>
